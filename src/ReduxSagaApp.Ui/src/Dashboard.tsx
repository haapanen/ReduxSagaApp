import * as React from "react";

export class Dashboard extends React.Component {
    render(): JSX.Element {
        return (
            <div>
                <h1>Dashboard</h1>
            </div>
        );
    }
}