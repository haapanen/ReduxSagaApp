import * as React from "react";

export class Dashboard extends React.Component {
    render(): JSX.Element {
        return (
            <div>
                <h2>Dashboard</h2>
            </div>
        );
    }
}